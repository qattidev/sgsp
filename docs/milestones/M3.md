# M3 — bounded messaging and APIs

Status: implementation gate verified on the current worktree; M8 release
evidence remains separate and incomplete.

The implementation is committed in the project history. The public API, typed codecs, borrowed
incoming ownership, request/reply, custom streams, polling and handler
dispatch, queue limits, global budget, slow-consumer handling, and bounded
observer aggregation are implemented in the root package and
`internal/runtime`.

Covered Architecture.md section-13.2 IDs: `TestSendOwnership`,
`TestReceiveOwnership`, `TestBidirectionalRequests`,
`TestUnknownRequestOutcome`, `TestPreSendFailure`, `TestUnknownRoutes`,
`TestTypedCodecs`, `TestQueueBackpressure`, `TestSequencedCoalescing`,
`TestWrongDispatchMode`, `TestDispatchOrdering`, `TestHandlerPanic`,
`TestSlowConsumer`, and `TestGlobalBudget`.

Current evidence, executed on 2026-09-22 with Go 1.27.1 on linux/amd64:

```text
go test -race -count=1 -run '^(TestSendOwnership|TestReceiveOwnership|TestBidirectionalRequests|TestUnknownRequestOutcome|TestPreSendFailure|TestUnknownRoutes|TestTypedCodecs|TestQueueBackpressure|TestSequencedCoalescing|TestWrongDispatchMode|TestDispatchOrdering|TestHandlerPanic|TestSlowConsumer|TestGlobalBudget)$' .
  selected root tests: 14; exit 0; duration 1.257s
go test -race -count=1 ./internal/runtime
  selected package tests: 6; exit 0; duration 1.037s
```

No benchmark artifact is used as API/runtime evidence. The source tests are
the authoritative regression records; M8 contains the separate retained
performance artifacts.
