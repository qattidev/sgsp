# M4 — authentication and initial admission

Status: implementation gate verified on the current worktree; continuity and
release-evidence gates are tracked by later milestones.

Revision: `084aa73fcca2cfe3a4bc4e80542e341884b5fe91`, with the current
uncommitted implementation worktree. The server implements authenticated
HELLO/WELCOME admission, authentication barriers, authenticated event/request
and custom-stream setup, expiration, revocation, credential refresh, JWT
helpers, admission signing/verification, and binding checks. No public
unauthenticated-session constructor is exposed.

Covered Architecture.md section-13.3 IDs: `TestAuthenticationBarrier`,
`TestJWTValidation`, `TestAuthRefresh`, `TestRevocation`, and
`TestAdmissionBinding`; related rejection/expiration cases are
`TestRejectedCredentialsNeverCreateSession` and
`TestAuthenticationExpirationClosesSession`.

Current evidence, executed on 2026-09-22 with Go 1.27.1 on linux/amd64:

```text
go test -race -count=1 -run '^(TestAuthenticationBarrier|TestRejectedCredentialsNeverCreateSession|TestAuthRefresh|TestAuthenticationExpirationClosesSession|TestRevocation)$' .
  selected root tests: 5; exit 0; duration 2.345s
go test -race -count=1 ./auth/jwt
  selected package tests: 2; exit 0; duration 1.056s
go test -race -count=1 -run '^TestAdmissionBinding$' .
  selected root tests: 1; exit 0; duration 1.095s
```

The commands used real localhost QUIC where the endpoint tests require it.
They do not include credentials in their output or retained benchmark
artifacts.
