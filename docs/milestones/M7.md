# M7 — executable integration example

Status: complete. The implementation, automated example suite, and retained
fresh-checkout direct/bootstrap operator exercise pass. This closes M7 only;
M8's separate capacity and impairment evidence is still incomplete.

The implementation is committed in the project history. `examples/action` has a 128 Hz
authoritative loop, sequenced input/update traffic, an operation-ID reliable
command, typed inventory request/reply, raw stream transfer, epoch-aware game
inbox, direct mode, and two-owner bootstrap mode. Its README lists the local
CA/development-identity commands without requiring a REST service or database.

Covered scenarios: `TestActionDirectProtocol`,
`TestActionReconnectResumesAuthoritativeSession`,
`TestActionBootstrapTwoClientsShareOwner`, and
`TestActionOwnerTerminationDoesNotRelocateGroup`.

Current evidence, executed on 2026-09-22 with Go 1.27.1 on linux/amd64:

```text
go test -race -count=1 ./examples/action
  selected example tests: 4; exit 0; duration 7.658s
```

This proves the automated local-QUIC scenarios. The documented direct and
bootstrap commands were also run from a fresh checkout by the retained
operator runner:

```text
scripts/run-action-operator-evidence.sh artifacts/action-fresh-checkout-final-20260923
  source revision: 3bee074ec443da919459072df8b0611ca4e0525b
  direct client: exit 0; update, typed inventory reply, and stream echo observed
  bootstrap client: exit 0; same full exchange observed after group resolution
  owner/bootstrap process-group cleanup: complete; ports 45761--45764 released
```

The runner created a new clone beneath that repository-local artifact
directory, used the commands in the README through `go run`, retained the
client/server logs, environment, clone, local Go cache, and `status.json`, and
refused `/tmp` or a non-empty destination. Its controlled startup-failure
exercise is separately retained at
`artifacts/action-fresh-checkout-failure-retention-20260923/`; that record is
not success evidence, but verifies that an unsuccessful operator attempt also
keeps a failed status and its logs.

Additional local operator evidence, executed on 2026-09-23 from this working
checkout, is retained outside Git under `artifacts/`: direct mode used an
owner on `127.0.0.1:45666` with logs in
`artifacts/action-direct-20260923-2/`; its client completed the authoritative
update, typed inventory reply, and raw stream echo. Bootstrap mode started two
owners on ports 45671 and 45672 plus bootstrap on 45673, with logs in
`artifacts/action-bootstrap-20260923/`; the client resolved `match-1` to one
owner and completed the same exchange. These runs use local development
credentials; the fresh-checkout record above is the M7 operator evidence.
