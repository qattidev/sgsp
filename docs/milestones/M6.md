# M6 — placement and group lifecycle

Status: complete on the current worktree. The implementation and the required
memory, lifecycle, and disposable-PostgreSQL evidence have passed. The
separate M8 reference-host performance gate remains incomplete.

Implementation baseline revision: `084aa73fcca2cfe3a4bc4e80542e341884b5fe91`.
Current PostgreSQL integration-coverage revision: `36a04e7d81227c2bd68e2c96f56277eb9a80499b`.
The implementation is committed; it includes bootstrap resolve, deterministic
owner selection, in-memory registry/store, PostgreSQL store and migrations,
signed admission binding, group-close persistence hooks, owner-health behavior,
and direct-resume independence from placement.

Covered Architecture.md section-13.4 IDs: `TestConcurrentAssignment`,
`TestAssignmentVersionAndClose`, `TestAdmissionBinding`, `TestPlacementOutage`,
`TestUnhealthyOwner`, `TestOwnerRestart`, `TestGroupCloseRace`, and
`TestDraining`.

The nested integration module also has `TestMigrationReapplicationAndDrift`.
Its disposable-schema helper applies the embedded migration twice as a no-op;
the test then changes only that schema's recorded checksum and requires
`ErrMigrationDrift`. `TestConcurrentBootstrapResolve` creates two separate
PostgreSQL adapters and bootstrap instances sharing the same disposable
schema, then barrier-starts 100 resolves and requires one stored owner.
`TestBootstrapCloseRace` similarly barrier-starts 100 close calls across the
two bootstrap instances, requires every close to be idempotent, and requires a
later resolve to return `ErrGroupClosed`. Together these exercise the SQL
store's shared-winner and durable-closure paths rather than only direct store
calls. They compiled on the current worktree without a database connection,
but still require the configured disposable database to execute. Before
migration, the same helper holds four separate pooled connections and requires
each `current_schema()` to be the generated schema. It then verifies that
`sgsp_schema_migrations` belongs to that schema, not the database default
schema. These checks protect the required per-connection `search_path` setup.

Current evidence, executed on 2026-09-22 with Go 1.27.1 on linux/amd64:

```text
go test -race -count=1 ./placement ./placement/memory ./placement/postgres
  selected package suites: placement, memory, migrations; exit 0; duration 4.100s
```

The expanded nested module compiled and its URL-independent identifier test
passed on 2026-09-23:

```text
GOCACHE=artifacts/routine-verification-20260923/go-cache \
  go -C integration/postgres test -count=1 -run '^TestQuoteIdentifier$' ./...
  selected test; exit 0; duration 0.005s
GOCACHE=artifacts/routine-verification-20260923/go-cache \
  go -C integration/postgres test -run '^$' -count=1 ./...
  complete module compile; exit 0; no tests selected
GOCACHE=artifacts/routine-verification-20260923/go-cache \
  go -C integration/postgres vet ./...
  complete nested module; exit 0
```

Actual disposable-PostgreSQL evidence was then run on 2026-09-23 with the
repository-local runner from `9b7971b`, a local loopback-only `postgres:16`
container with no volume, Go 1.27.1 on linux/amd64, and clean source revision
`36a04e7d81227c2bd68e2c96f56277eb9a80499b`:

```text
SGSP_TEST_DATABASE_URL=REDACTED \
  scripts/run-postgres-integration.sh artifacts/postgres-integration-rerun-20260923
  go -C integration/postgres test -race -count=20 -timeout=12m -v ./...
  all six tests, including 100-way direct and bootstrap races; exit 0; 24.139s

GOCACHE=artifacts/routine-verification-20260923/go-cache \
  go test -race -count=20 ./placement ./placement/memory ./placement/postgres
  selected package suites; exit 0; placement 21.046s, memory 1.039s, postgres 1.023s
GOCACHE=artifacts/routine-verification-20260923/go-cache \
  go test -race -count=20 -v \
  -run '^(TestGroupCloseRace|TestOwnerRestart|TestPlacementOutage)$' . ./placement
  selected lifecycle tests; exit 0; root 2.570s, placement 1.023s
```

The `artifacts/postgres-integration-rerun-20260923/` campaign retains its
redacted environment, exact command, local cache, log, manifest, terminal
status, and summary. The preceding
`artifacts/postgres-integration-20260923/` failed run is also retained: the
initial unbounded test pool exceeded PostgreSQL's default client reservation
while 100 operations began concurrently. Commit `36a04e7` bounds the test
pool to 16 connections without reducing the 100 concurrent calls; the clean
rerun is the passing M6 result. The disposable container was removed after
the campaign. The nested module still intentionally fails rather than skips
when `SGSP_TEST_DATABASE_URL` is absent, preventing a memory-store test from
being represented as SQL conformance.
